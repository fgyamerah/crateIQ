======================================================================
DJTOOLKIT — NEXT TASKS / OPEN WORK
======================================================================

Post-audit stabilization note (2026-07-02):
- Full repository audit completed in `AUDIT_REPORT.md`.
- Frontend route/navigation consolidation completed: supported operational pages
  are mounted, legacy/placeholder pages redirect safely, and navigation matches
  the router.
- Frontend package metadata and documented install/typecheck/build commands were
  normalized for a clean workspace.
- Backend test environment stabilization completed: the dev requirements now
  include backend/TestClient dependencies, test runs use an isolated temporary
  pipeline/backend library root, and `python -m pytest -q` passes in Python 3.12.
- Next recommended task: add a reproducible local-runtime preflight with a
  non-secret environment template and smoke tests for supported UI/API routes.

Backend hang diagnosis note (2026-07-15):
- The original bounded run stalled at the first FastAPI health test because the
  restricted execution sandbox did not wake a cross-thread asyncio event loop,
  which is required by AnyIO/Starlette TestClient.
- A minimal bare FastAPI reproduction and the CrateIQ suite pass outside that
  sandbox with the same installed packages. No application lifecycle fix was
  warranted; this was pre-existing environment behavior, not the rename.
- The complete suite now passes twice: 860 tests per run, exiting normally.

Legend:
[ ] = open
[~] = in progress
[x] = done

Priority:
P0 = critical (affects core functionality)
P1 = high
P2 = medium
P3 = low


----------------------------------------------------------------------
CURRENT FOCUS

[x] (P1) Safe ffprobe audio-quality preview (2026-08-05)
         `/jobs` now runs a bounded, read-only JSON metadata probe on at most
         ten validated imported tracks when ffprobe is available. It returns
         only safe relative paths and neutral probe states; no transcode,
         media/tag/local-DB write, or remediation action exists.

[x] (P1) Safe rmlint duplicate detection preview (2026-08-05)
         `/jobs` scans a bounded set of validated imported paths only through
         rmlint JSON output. It returns duplicate groups for review with no
         delete/move/rename/quarantine/tag/file/DB action; resolution remains
         a future review workflow.

[x] (P1) Safe Beets enrichment preview (2026-08-05)
         `/jobs` now reviews only local-index rows missing artist/title/genre.
         It never invokes beet, modifies tags/files, or applies suggestions;
         selected-field DB-only acceptance remains a future review workflow.

[x] (P0) Safe key/Camelot analysis runner (2026-08-05)
         `/jobs` now supports a preview-first, explicitly confirmed
         keyfinder-cli-only key pass. Only rows missing both key fields are
         eligible; recognized values use the existing Camelot mapping and are
         saved to local DB with lower authority. No tags/media are written and
         MIK/trusted/existing keys cannot be overwritten.

[x] (P0) Safe aubio BPM analysis runner (2026-08-05)
         `/jobs` now supports a preview-first, explicitly confirmed aubio-only
         BPM pass with a 1–50 track limit. Only null BPM records are eligible;
         accepted 40–250 BPM values are written to `processed.db` with
         lower-authority `aubio` provenance. No tags/media are written and
         MIK/trusted/existing BPM cannot be overwritten.

[x] (P0) Analysis Jobs catalog and candidate-preview foundation (2026-08-05)
         `/jobs` now presents MIK coverage plus BPM/key, Beets, duplicate, and
         quality workflow cards. It reads only local-index candidates; missing
         tools gate only their own card; MIK import stays explicit in Settings;
         all other runners are clearly preview-only/pending with no job/tool
         launch, tag/file, or DJ database writes.

[x] (P0) Mixed In Key-compatible coverage and DB-only import foundation
         (2026-08-05). Settings can now run explicit read-only tag previews
         and import only missing BPM/key values into CrateIQ's local index with
         `mik_compatible_tag` provenance. Existing values are never
         overwritten, no external analyzer runs, and cue-tag support is
         honestly reported as unavailable. MIK remains optional for every core
         workflow.

[x] (P0) Optional analysis settings and per-workflow capability gating
         (2026-08-05). Settings now separates always-ready core workflows from
         tool/source-specific advanced capability cards, persists default-off
         BPM/key and external-tool preferences, and locks MIK preservation,
         existing BPM/key/cue preservation, and missing-data-only behavior.
         Import remains analysis-free; unavailable tools disable only their
         named future workflow.

[ ] (P1) Add safe persisted Analysis Jobs history/progress
         Record only explicit BPM/key DB-only runs with result summaries,
         cancellation semantics where needed, and no pipeline/tag/file writes.

[x] (P1) Full functionality, workflow, dependency, and product-surface audit
         (2026-08-05). `docs/audits/CRATEIQ_FUNCTIONALITY_WORKFLOW_AUDIT.md`
         inventories the supported UI/API/CLI surface, external-tool contracts,
         standalone usability, MIK gaps, optional-analysis design, first-run
         workflow, route/sidebar UX, safety risks, phased roadmap, and the next
         three implementation prompts. No runtime or library behavior changed.

[x] (P1) Library Initialization / Import Wizard foundation (2026-08-05)
         Settings now initializes a pending configured root with CrateIQ's
         local `logs/processed.db` index only. Explicit preview scans report
         audio filenames without writes; confirmed import writes local index
         records only. Metadata analysis and all music/tag/MIK/DJ app changes
         remain deferred.

[x] (P1) Library Import Wizard UX polish (2026-08-05)
         Settings now presents the first-use path as select → initialize →
         scan preview → review → import, with clear local-index-only safety
         copy, relative samples, counts, warnings, import idempotency status,
         and next actions. Empty libraries route to this flow instead of
         suggesting analysis before tracks are imported.

[x] (P1) Local-first Settings page (2026-08-05)
         `/settings` now exposes selected-library, optional-tool, runtime, and
         locked safety diagnostics. Its only editable value is the persisted
         default export path mode in ignored `logs/app_settings.json` plus a
         validated pending library root in ignored `.run/local/crateiq.env`.
         The service helper uses that root only on a configured-library restart;
         binary overrides remain read-only process-start environment settings.
         No scanning or library/DJ application mutation is introduced.

[x] (P1) Audio Preview Player foundation (2026-08-04)
         Browser-native preview is available from Library, Manual Crates, and
         Smart Crates through a scoped DB-backed endpoint. It only serves
         existing files below the selected root, supports seek ranges, and
         does not scan/transcode or alter audio/tags/BPM/key/cues/MIK/DJ data.
         Missing demo placeholder audio is shown as unavailable; waveform,
         cue, beat-grid, and keyboard controls remain future work.

[x] (P1) Rekordbox XML Export foundation (2026-08-04)
         Saved Manual Crates now support preview-first, staged XML exports
         under `<library-root>/exports/rekordbox/`, with ordered collection and
         playlist nodes. Files are for manual XML import only; no live
         Rekordbox database/device/USB path is writable in this workflow.
         No music, metadata, BPM/key/cues, MIK, or Serato data is modified.

[x] (P1) Serato Export / Crate Writer foundation (2026-08-04)
         Saved Manual Crates now support preview-first staged Serato handoff as
         M3U8 plus manifest under `<library-root>/exports/serato/`. The flow
         never writes live `_Serato_` folders, never replaces outputs, and
         does not claim binary `.crate` support; direct writers are deferred.
         No music, metadata, BPM/key/cues, or MIK data is modified.

[x] (P1) Crate Export Foundation (2026-08-04)
         Manual Crates can be previewed/exported as CSV, JSON, M3U, or M3U8
         under `<library-root>/exports/`, with filename/relative/absolute path
         modes and no-overwrite output naming. This is portable-file-only;
         Rekordbox-specific writers remain a future phase.

[x] (P1) Smart Crates / Smart Playlists suggestions (2026-08-04)
         Deterministic local previews now filter/rank existing BPM, Camelot,
         genre, and issue data, then explicitly save to editable Manual Crates.
         Energy/vibe and date-added remain unavailable until trusted track
         fields exist; no files, metadata, MIK values, or exports are changed.

[x] (P1) Manual crates / playlists (2026-08-04)
         Local-first manual crate CRUD, ordered track membership, demo crates,
         focused API coverage, and the `/crates` frontend workflow are complete.
         State is scoped to `<library-root>/logs/manual_crates.db`; it never
         touches files, tags, BPM/key/cues, or Mixed In Key data. This is the
         foundation for later smart-playlist and export integration.

[x] (P1) Document optional local music-analysis tooling and readiness guidance
         (2026-07-29): added docs/operations/LOCAL_TOOLING.md, explicit blank
         KEYFINDER_BIN/AUBIO_BIN/BEET_BIN PATH-search defaults in .env.example,
         and focused preflight override coverage. Beets remains an optional
         `beet` CLI runtime tool, not a Python requirements dependency;
         keyfinder-cli remains a manual/package/build installation depending
         on the Linux distribution. No scanning or pipeline behavior changed.
[x] (P1) Document a safe Linux Mint/Ubuntu keyfinder-cli source-build path
         (2026-08-02): verified the upstream keyfinder-cli v1.2.0 and
         Mixxx-maintained libkeyfinder 2.2.8 sources; documented a pinned,
         user-local, source-only CMake build with the needed Ubuntu/Mint
         development packages and KEYFINDER_BIN readiness check. No helper,
         automated package installation, binaries, or pipeline changes added.
----------------------------------------------------------------------
[x] (P1) Library view: split out of CrateMind.tsx + mockup-faithful visual
         pass (2026-07-26) — frontend/src/components/library/* (new),
         compact LibraryRuntimeStrip replacing the global banner on "/",
         6-card overview row (added tracks_analyzed to
         GET /api/library/overview), Quality-meter table column,
         auto-selected first row, always-populated inspector, camelot-hue
         color coding, Inter font app-wide, scripts/seed_demo_library.py
         (52 demo tracks). See CHANGELOG.txt 2026-07-26 entry.
[x] (P3) Inspector: add a real Camelot-wheel graphic next to the BPM/Key/
         Camelot tiles (2026-07-26) — frontend/src/components/library/
         CamelotWheel.tsx (SVG, 24 wedges, highlights selected/adjacent/
         relative keys + API-returned compatible keys, accessible aria-
         label + aria-describedby, graceful "no key" state). See
         CHANGELOG.txt 2026-07-26 "Compatible-tracks API + real Camelot
         wheel" entry.
[x] (P1) App-wide visual system rollout from the Library view (2026-07-26)
         Extended the Library view's visual system to the rest of the app via
         shared CSS tokens/primitives instead of a per-page redesign. New:
         frontend/src/components/ui/{StatusStrip,KpiCard,EmptyState,Badge}.tsx.
         ReadinessBanner.tsx now renders <StatusStrip>. Fixed both
         Impeccable-flagged side-tab-border warnings (Export.tsx, SsdSync.tsx)
         via StatusStrip. Removed ~26 leftover pre-teal-swap blue color
         instances across index.css (Metadata Repair/Sanitation, Quality, Set
         Builder, CrateMind workspace) that were never migrated when --accent
         changed from blue to teal. Added a shared atmospheric gradient wash
         to .app-main so every non-Library/CrateMind page gets the same
         background treatment. See CHANGELOG.txt 2026-07-26 entry for full
         detail. No backend/pipeline/route/API changes.
[x] (P2) Adopt KpiCard/EmptyState/Badge — the visual rollout's three unused
         shared primitives (2026-07-26). Quality.tsx: top metric row now uses
         KpiCard (ring-progress for the 3 percentage metrics, icon-based for
         the other 2); HIGH/MEDIUM/LOW confidence chips now use Badge (also
         fixed a real pre-existing bug — `.conf-chip--high/medium/low` had no
         CSS, so all three tiers rendered identically uncolored); "Recommended
         Next Actions" now uses EmptyState when genuinely empty instead of a
         silent blank grid. BpmReview.tsx: both "no anomalies" messages now
         use EmptyState (same copy, same trigger conditions). KpiCard/Badge
         were deliberately NOT forced into BpmReview — no metric-card grid,
         and its anomaly reason/status labels already have a working, more
         granular color taxonomy than Badge's 6 tones support. See
         CHANGELOG.txt 2026-07-26 entry for full detail.
[x] (P2) Follow-up from the 2026-07-26 visual rollout — completed 2026-08-02.
         Jobs.tsx (StatusStrip + EmptyState + React key fix), SetBuilder.tsx
         (StatusStrip for both error sites, EmptyState for no-saved-sets,
         Badge for job-status/Camelot-key/vibe chips), and Reconciliation.tsx
         (Badge-based LedgerBadge, StatusStrip for both error sites,
         EmptyState x3, index-based validation-records key replaced with a
         stable content-derived key) all adopted the shared primitives where
         they naturally fit — see CHANGELOG.txt 2026-08-02 entries.
         Deliberately left bespoke: phase-badge's 5-color semantic taxonomy,
         set-stat-card/recon-stat detail tiles (KpiCard not forced — compact
         detail-panel tiles, not page-level overview rows), live-indicator
         chips, loading text, and inline table-cell fallback messages.
[x] (P3) Jobs.tsx: pre-existing React "missing key prop" warning in
         JobsTable's rendered list — fixed 2026-08-02. Root cause:
         jobs.map() returned a <> fragment with the keys on the inner
         <tr> elements; React requires the key on the map callback's
         top-level element. Now <Fragment key={job.id}> (stable real job
         ID, no array index). Verified gone via headless Chrome against
         the demo library.
[ ] (P3) Table: mockup's "Energy" column is a 5-bar horizontal EQ meter;
         current "Quality" column is a compact vertical 4-bar meter driven
         by the real quality_tier field. Revisit only if an actual
         per-track energy/loudness metric becomes available — do not
         fabricate one to match the mockup's shape.
[x] (P2) Library view redesign — emerald/teal/cyan/violet theme (2026-07-25)
         frontend/src/pages/CrateMind.tsx (library section), Sidebar.tsx,
         index.css. Status strip, filter chips, overview cards, table
         columns (#, Key, Camelot, Quality), inspector stat tiles/waveform
         placeholder/compatible-tracks deferred note, sidebar badges +
         Library Health card. Fixed a pre-existing sidebar overflow bug and
         reconciled inspector-stacking responsive behavior at <=860px
         (previously the inspector was hidden outright below that width).
         See CHANGELOG.txt for the full functionality classification.
[ ] (P3) Wire genre/BPM/has-key filter chips into the Issues/Enrichment
         sections too if useful, or keep them Library-only by design —
         currently scoped to the library UI section only.
[x] (P3) Expose a per-track "compatible tracks" (harmonic match) read API
         so the Library inspector's deferred placeholder can go live
         (2026-07-26) — GET /api/tracks/{id}/compatible, reusing
         modules/harmonic.py's existing Camelot/BPM/genre scoring (no
         duplicate algorithm). Same-key/adjacent/relative-key matching
         only; energy-tier boost remains deferred (no persisted energy
         field yet). See CHANGELOG.txt 2026-07-26 entry.
[ ] (P2) Follow-up: extract intelligence/harmonic/ pure engine from
         modules/harmonic.py per docs/strategy roadmap Phase 4, add
         notation conversion (Camelot ↔ traditional ↔ Open Key) and half/
         double-time BPM awareness, and switch the compatible-tracks
         endpoint's genre scoring to the taxonomy-driven relations instead
         of the hardcoded _GENRE_RELATIONS — deferred out of the 2026-07-26
         compatible-tracks pass to keep that change minimal/reuse-only.
[ ] (P3) docs/generated/repo_file_index.md is stale (predates this
         session): missing the entire frontend/src/components/library/*
         split from 2026-07-25/26 and this session's CamelotWheel.tsx.
         Regenerate via tools/static_analysis/generate_repo_inventory.py
         when convenient — not run as part of the 2026-07-26 compatible-
         tracks change to keep that diff focused.
[ ] (P3) Expose a duplicates count via a read API (dedupe is CLI-only
         today) so the Library overview "Duplicates" card can show a real
         number instead of "Not available".
[x] (P0) Document crateIQ target product vision and capability roadmap (2026-07-24)
         docs/strategy/CRATEIQ_PRODUCT_VISION_AND_ROADMAP.md — current state,
         gap analysis, metadata/genre/harmonic/player/playlist/export strategy,
         9-phase capability roadmap mapped to docs/CRATEIQ_ROADMAP.md.
[x] (P1) Implement local-runtime preflight and readiness contract (2026-07-24)
         backend/app/core/preflight.py (read-only checks: root safety,
         pipeline DB, entrypoint, backend data dir, 7 external binaries) +
         GET /api/runtime/readiness + tests/test_preflight.py + expanded
         .env.example. Frontend banner intentionally deferred (see below).
[x] (P2) Frontend readiness banner: surface /api/runtime/readiness as a small
         dismissible banner in the Layout when status is not "ready" (2026-07-25)
         frontend/src/api/runtime.ts (client), frontend/src/hooks/useReadiness.ts
         (single fetch on Layout mount, no polling), frontend/src/components/
         ReadinessBanner.tsx (warn styling for degraded, error styling for
         not_ready, neutral notice if the fetch itself fails, session-scoped
         dismiss, top 3 failing/warning checks only, no raw JSON/paths).
         Wired into Layout.tsx above <Outlet/>. No routing changes, no backend
         changes.
[x] (P2) Local service helper: scripts/crateiq-local-services.sh (2026-07-25)
         start/stop/restart/status/logs subcommands + crate_* shell functions
         via `source ... --aliases`. Ports: backend 8020, frontend 5175
         (LedgerIQ keeps 5173/8000; OpsIQ uses 5174/8010). PID files/logs in
         .run/ (gitignored). Dev proxy overridable via
         CRATEIQ_API_PROXY_TARGET (default unchanged).
[ ] (P3) Resolve duplicate Vite config: frontend/vite.config.js shadows
         frontend/vite.config.ts (Vite resolves .js first). Both currently
         carry identical config. Decide which to keep and delete the other
         (needs explicit approval to delete a tracked file).
[x] (P3) Repo hygiene: stop tracking generated TypeScript build metadata (2026-07-25)
         frontend/tsconfig.app.tsbuildinfo and frontend/tsconfig.node.tsbuildinfo
         removed from tracking via `git rm --cached`; `*.tsbuildinfo` added to
         .gitignore (NODE / FRONTEND section). Typecheck/build no longer dirty
         the working tree. Flagged during the readiness-banner review.
[ ] (P3) Wire readiness awareness into the Jobs page (disable submission
         hints when relevant binaries are missing) — optional polish.
[ ] (P1) Metadata model extension (strategy Phase 2): additive tracks columns
         for identity + structured artist/title/version, field_provenance
         table, optional fpcalc fingerprinting — after preflight lands.
[ ] (P1) Genre intelligence layer (strategy Phase 3): genre_taxonomy.json,
         genre_mappings.json, deterministic resolver, genre-normalize
         preview/apply command, review integration.
[ ] (P2) Extract intelligence/harmonic pure engine from modules/harmonic.py
         with golden-file tests pinning current set-builder scoring first
         (strategy Phase 4).
[ ] (P2) Promote enrichment response caching (provider, query-hash, TTL)
         from P3 to pre-MusicBrainz requirement (strategy section 7).
[x] (P0) CrateIQ fork foundation, identity rename, baseline audit, and roadmap (2026-07-14)
         Upstream retained as `upstream`; no `origin`; work isolated on
         feat/crateiq-foundation-audit. Future phases are documented only.
[x] (P0) Diagnose the baseline FastAPI health-test hang (2026-07-15)
         The exact test stalled before TestClient entered. A minimal AnyIO/
         asyncio reproduction isolated the failure to the restricted execution
         sandbox's cross-thread event-loop wakeup. Host verification passes.
[ ] (P1) Review and approve docs/CRATEIQ_ROADMAP.md before Phase 1 implementation
[ ] (P1) Implement Phase 1 Foundation and design system only after review
[x] (P1) Resolve COMMANDS.md / commands.md macOS case conflict (2026-07-02)
         COMMANDS.md remains canonical; the distinct legacy DJ Toolkit guide is
         archived at docs/operations/LEGACY_DJ_TOOLKIT_COMMANDS.md.
[x] (P1) Post-audit frontend route/navigation consolidation (2026-07-02)
         Supported: library/quality/issues/enrichment/repair/sanitation/BPM/audit/
         folders/jobs/set-builder/exports/sync/reconciliation.
         Hidden via redirects: legacy Dashboard, Collection, Tracks, and placeholder Settings.
[x] (P1) Backend test environment stabilization (2026-07-02)
         requirements-dev includes pipeline/backend/test dependencies; tests
         isolate both library-root environment variables; 860 tests pass twice
         under Python 3.12 in the normal host environment.
[x] (P1) Local-runtime preflight and smoke-test contract (2026-07-24)
         Preflight + non-secret env example + readiness endpoint, plus
         tests/test_supported_route_contracts.py: all 14 supported frontend
         routes mapped to their primary read-only backend endpoints, with
         App.tsx route-drift detection, missing-DB degradation checks, and a
         no-subprocess/read-only guard. Mutating endpoints (job submit/cancel,
         sync preview/run, export validate/run, review actions, apply paths,
         bpm-check/reanalyze, set-builder dispatch, reconciliation
         validate-plan) are intentionally NOT smoke-tested; they are listed in
         DEFERRED_ENDPOINTS in the test file.
[ ] (P2) Contract coverage for deferred mutating endpoints
         Design safe fixture-based tests for the POST surfaces listed in
         DEFERRED_ENDPOINTS (e.g. exports/validate and reconciliation/
         validate-plan are in-process and could be tested with fixtures;
         job-spawning and rsync endpoints need a mocked runner layer).
[ ] (P2) Resolve FastAPI/Starlette TestClient HTTP client deprecation warning
         Evaluate the supported httpx2 migration separately; current tests pass.
[ ] (P2) Frontend build-tool dependency upgrade
         Plan and test the Vite major upgrade required to clear the remaining
         development-only npm audit advisories; do not use npm audit fix --force.
[~] (P0) Phase 7 Full Reconciliation — next recommended phase after README Phase 1-8 platform milestone (2026-05-06)
         README.md has been updated for the Phase 1-8 platform milestone.
         Current stable commit hash: b4c6ffb4048c4c98d225f6c65e40a7cce7f1a8e3
         WARNING: back up <root>/logs/processed.db before any reconciliation apply work.
         Scope reminder: design and test reconciliation apply behavior before enabling writes.
[x] (P1) artist-repair-review — human approval workflow; queue state persistence; 158 tests (2026-05-03)
[x] (P1) bare-number guard v6 — numeric/ordinal second token preserved; 71 tests (2026-05-03)
[x] (P1) title-number-recover — filename-based recovery for bad bare-number strips; number>=10 guard; v5 rules (2026-05-03)
[x] (P1) metadata-sanitize-rollback — revert bad title_bare_number_stripped with bare-number guard (2026-05-03)
         _BARE_NUM_PROTECTED_WORDS guard; v4 rules; run_metadata_sanitize_rollback; 8 new tests
[x] (P1) metadata-sanitize — new title rules: bare number strip, trailing BPM, domain tokens, label suffix, feat casing (2026-05-02)
         _strip_label_suffix() helper; protected-paren guard; 37 new tests in test_metadata_sanitize.py
[x] (P1) artist-repair subcommand — detect merged artist names before artist-intelligence (2026-05-02)
         modules/artist_repair.py; HIGH/MEDIUM/LOW confidence; review queue; --move-artist-review;
         36 tests in test_artist_repair.py; pipeline order: sanitize → repair → intelligence
[ ] (P2) artist-repair: HIGH-confidence auto-apply on --apply (currently write-eligible but not yet wired through tagger)
[ ] (P2) artist-repair: build-known-artists scan uses folder depth 1+2; add tag-sample depth option for deeper libraries
[ ] (P2) artist-repair review queue approval workflow (artist-repair --approve-queue)
[x] (P1) metadata-sanitize command — offline deterministic tag sanitation (2026-04-18)
[x] (P1) metadata-sanitize → ai-normalize integration via --pre-sanitize flag (2026-04-19)
[x] (P1) Collection workspace UI — Phase 1 static shell (2026-04-19)
[x] (P1) Collection workspace UI — Phase 2: interactions, search, sort, presets, responsive (2026-04-19)
[x] (P1) Collection workspace UI — Phase 3: metadata-sanitize live job (2026-04-19)
[x] (P1) Collection workspace: real filesystem directory tree via API (2026-04-19)
[x] (P1) Collection workspace — Phase 4: real tracks + counts + maintenance + sidebar collapse (2026-04-19)
[x] (P1) Collection workspace — run results browser: Results panel in Collection UI (2026-04-23)
[x] (P1) Library maintenance — dedupe preview-first + quarantine with source_root (2026-04-23)
[x] (P1) Library maintenance — orphan-scan: stale_db_rows + untracked_files (2026-04-23)
[x] (P1) Library maintenance — quarantine path isolation: MUSIC_ROOT/QUARANTINE, skip dirs, nested-move guard (2026-04-23)
[x] (P1) Operational storage refactor: all maintenance paths consolidated under MUSIC_ROOT/.BIN/ (2026-04-23)
[x] (P2) tests: dedupe fixture generator — 9 controlled test cases, mutagen-parseable stubs (2026-04-23)
[x] (P1) filename-normalize subcommand — rename files to {artist} - {title} ({version}).ext (2026-04-24)
[x] (P1) filename-normalize — artist review queue + --move-artist-review flag (2026-04-25)
[x] (P1) dedupe — quarantine placed under --path library root, not global /music/.BIN (2026-04-25)
[x] (P1) library-organize subcommand — sort into letter/artist folders, processed-state tracking (2026-04-25)
         data/review/artist_review_queue.jsonl; .BIN/ARTIST_REVIEW/ quarantine; album in tags
[x] (P1) Persistent per-stage processed-state tracking — skip unchanged files, --force, --reset-stage (2026-04-23)
         db.py: processed_state table + 4 helpers; modules/run_logger.py (new);
         integrated into metadata-sanitize, ai-normalize, artist-intelligence,
         metadata-enrich-online, filename-normalize; pipeline.py subparser flags;
         COMMANDS.md Incremental Processing section; README.md note
[ ] (P2) Collection workspace — Phase 5: pagination for large folders (>500 tracks)
[ ] (P2) Collection workspace — duplicate scan review UI (show found dupes in table)
[ ] (P2) Collection workspace — virtual list for very large track sets
[ ] (P2) Library maintenance — expose orphan-scan results via API endpoint (for Collection UI)
[ ] (P3) Library maintenance — quarantine browser in Collection UI (review quarantined files)
[x] (P1) Artist Intelligence — Phase 2 hardening complete (2026-04-19)
[x] (P1) Artist Intelligence — Phase 3: pollution guards, feat dedup, personal-name split (2026-04-19)
[x] (P1) Artist Intelligence — Phase 4: nested separator split, casing normalization, confidence boost (2026-04-19)


----------------------------------------------------------------------
ONLINE ENRICHMENT (completed 2026-04-18)
----------------------------------------------------------------------
[x] (P1) intelligence/enrichment/ package: schema, matcher, Spotify, Deezer, runner
[x] (P1) enrichment: review queue CLI + move-ignored operational features (2026-04-20)
         review-queue subcommand, _enqueue_for_review, _move_to_ignored, --move-ignored flag
[x] (P1) metadata_matcher: title normalization — non-trailing label brackets, bare BPM, dupe collapse (2026-04-20)
[x] (P1) metadata_matcher: album filter + title pre-clean + label reason codes (2026-04-20)
         album_rejected_compilation, title_cleaned_for_match, label_match_boost/label_alias_boost
[x] (P1) metadata_matcher: multi-artist subset matching + version tolerance (2026-04-20)
         subset_artist_match (0.88), partial_artist_match (0.82/logged), version_missing_tolerated
[x] (P1) metadata-enrich-online: strict multi-factor validation layer (2026-04-20)
         Gates: title_sim>=0.92, artist_sim>=0.85, version alignment
         Scoring: title(50%)+artist(30%)+version(20%), label as booster
         Thresholds: apply>=0.90, review 0.75-0.89, skip<0.75
         decision_code on EnrichmentMatch, canonical artist from artist-intelligence
[x] (P1) metadata-enrich-online CLI subcommand wired into pipeline.py
[x] (P1) config.py: SPOTIFY_CLIENT_ID, SPOTIFY_CLIENT_SECRET, ENRICH_ONLINE_MIN_CONFIDENCE
[x] (P1) Dataset JSONL logging: enrichment_queue, enrichment_accepted, enrichment_rejected
[x] (P1) Lower default threshold 0.92 → 0.80 (config.py + runner.py + pipeline.py)
[ ] (P3) metadata-enrich-online: duration booster — add duration_ms to EnrichmentCandidate
         and parse from Spotify/Deezer/Traxsource responses (+0.03 boost when within 3s)
[x] (P1) Version mismatch safeguard: block apply when both titles carry different version tokens
         (metadata_matcher.py: _extract_version_label + best_match() safeguard block)
[x] (P1) Scoring weights rebalanced: title 0.45→0.30, version 0.10→0.25
[x] (P1) Hard safeguards: version mismatch cap 0.74, artist sim < 0.90 cap 0.74
[x] (P1) Label alias boost: +0.05 when labels are known aliases (13 groups seeded)
[x] (P2) Traxsource lookup (traxsource_lookup.py) — dance-music specialist fallback
         Genre-aware trigger: house/Afro/deep/soulful → always try Traxsource
         Confidence-based trigger: best_conf < 0.80 → try Traxsource
[x] (P2) EnrichmentMatch.sources_tried — tracks which APIs were queried
[x] (P2) EnrichmentCandidate.genre — Traxsource genre/subgenre field
[x] (P2) Junk metadata cleaner pre-enrichment step (is_junk_album + _clean_junk_fields)
[x] (P2) --clean-junk-only CLI flag (skip API, clean only)
[ ] (P2) Deezer/Spotify album-detail call to retrieve record label
[ ] (P3) Traxsource: verify CSS selectors against live site before production use
         (selectors in _SEL_* constants in traxsource_lookup.py)
[ ] (P3) Cache Spotify/Deezer/Traxsource results to avoid re-hitting APIs on re-runs
[ ] (P3) ISRC → MusicBrainz lookup as a zero-credential fallback


----------------------------------------------------------------------
REPO STRUCTURE (completed 2026-04-18)
----------------------------------------------------------------------
[x] (P1) Move artist_intelligence/ → intelligence/artist/
[x] (P1) Move label_intel/ → intelligence/label/
[x] (P1) Update all imports: artist_intelligence.* → intelligence.artist.*
[x] (P1) Update all imports: label_intel.* → intelligence.label.*
[x] (P1) Remove djtoolkit_label_intelligence_feature/ (legacy scaffold)
[x] (P1) Remove sample_codes/ (scratch files)
[x] (P2) Remove preview.txt and root-level preview JSON files from tree


----------------------------------------------------------------------
REVIEW DATASET / FEW-SHOT PIPELINE (completed 2026-04-18)
----------------------------------------------------------------------
[x] (P1) ai/review_dataset.py — write_queue_entry, write_accepted, write_rejected,
         build_fewshot, load_fewshot_examples
[x] (P1) Stub JSONL files in data/intelligence/ (5 files)
[x] (P1) config.py — AI_REVIEW_QUEUE, AI_ACCEPTED_EXAMPLES, AI_REJECTED_EXAMPLES,
         AI_REVIEW_DECISIONS, AI_FEWSHOT_EXAMPLES paths
[x] (P1) pipeline.py — build-fewshot subcommand
[x] (P1) ai/normalizer.py — hooked dataset writes into run_ai_normalize (--apply mode)
[ ] (P2) Inject fewshot_examples.jsonl into Ollama prompt via _build_prompt()
[ ] (P2) build-fewshot: add --min-confidence filter flag
[ ] (P3) Interactive review workflow: review_decisions.jsonl populated by a
         future 'ai-review' CLI command


----------------------------------------------------------------------
ARTIST INTELLIGENCE (Phase 2 hardening complete 2026-04-19)
----------------------------------------------------------------------
[x] (P1) artist_intelligence package: schema, normalizer, parser, alias store, runner
[x] (P1) CLI: artist-intelligence subcommand wired into pipeline.py
[x] (P1) Config: ARTIST_ALIAS_STORE, ARTIST_REVIEW_QUEUE paths added
[x] (P1) Seed alias: Heavy K / Heavy-K in data/intelligence/artist_aliases.json
[x] (P1) Phase 2 hardening: structured change reasons, conservative alias, feat normalization
[x] (P1) Phase 3 hardening: multi-artist split, pollution guards, feat dedup (2026-04-19)
         ArtistAliasStore.lookup_with_method() tracks exact/normalized/ci match type.
         CI-only alias matches cap confidence to 0.70 → go to review queue as skipped_ambiguous_alias.
         _propose_artist uses direct == (not names_are_equivalent) so feat_normalized changes surface.
         _compute_change_reasons: feat_normalized, alias_resolved, separator_normalized,
         spacing_fixed. Reasons in diff, preview, JSON output, APPLIED log line, summary breakdown.
         47 unit tests in tests/test_artist_intelligence.py (all passing).
[ ] (P2) Phase 3: populate alias store from existing library scan (auto-discover variants)
[ ] (P2) Phase 3: add --add-alias CLI flag (artist-intelligence --add-alias "Heavy-K" "Heavy K")
[ ] (P2) Phase 3: review queue approval workflow (artist-intelligence --approve-queue)
[ ] (P3) Future: label_intelligence package mirroring this same pattern


----------------------------------------------------------------------
DOCUMENTATION (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P2) README.md — full rewrite: web app, all API routes, folder structure,
         two-terminal start, troubleshooting, next steps, current status table
[x] (P2) CHANGELOG.txt — docs entry added
[x] (P2) DJToolkit_CONTEXT.txt — backend section expanded to reflect v0.6.x
         (process_registry, SSD sync, BPM analysis, all 7 route files, frontend)


----------------------------------------------------------------------
FRONTEND UI POLISH (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) Fix .log-inline CSS — all inline log views now constrained (max-height 55vh,
         overflow-x/y: auto, white-space: pre, monospace font). Affected: SetBuilder,
         Export, SsdSync. Previously the class had zero CSS, causing overflow.
[x] (P2) Sidebar icons — lucide-react SVG icons replace Unicode glyphs
[x] (P2) Sidebar active state — 2px left-border accent on active link
[x] (P2) Font upgrade — IBM Plex Sans loaded via Google Fonts; replaces system-ui
[x] (P2) .badge--info — added for Camelot key badges in SetBuilder
[x] (P2) .table-wrap — added table/th/td rules; SetBuilder bare tables now styled
[x] (P2) PageHeader component — reusable title/subtitle/badge/actions; applied to all pages
[x] (P2) Jobs page — lucide buttons, JobProgress bar (numeric + indeterminate), running-row accent
[x] (P2) SetBuilder — collapsible log section, set-detail stat cards
[x] (P2) SsdSync — mounted status badge in PageHeader
[x] (P2) Table polish — improved header contrast (#8490b0), row padding, rgba borders
[x] (P2) spin/@keyframes — Loader2 spinner on async buttons (Jobs, BpmReview, Export, SsdSync)

[ ] (P2) Dashboard: improve stat cards (left-border accent coloring, tighter layout)
[ ] (P2) Export: output path copy-to-clipboard button
[ ] (P3) BpmReview: collapsible re-analysis log panel (same pattern as SetBuilder)
[ ] (P3) SsdSync: make sync history use .table/.table-wrapper for consistency
[ ] (P3) Dashboard: show last-run timestamp + quick-launch buttons for common commands


----------------------------------------------------------------------
EXPORT WORKFLOW (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) POST /api/exports/validate — in-process validation, no subprocesses
[x] (P1) POST /api/exports/run — dispatch rekordbox-export job (async, returns job_id)
[x] (P1) GET /api/exports — list past export jobs (filtered view of jobs table)
[x] (P1) GET /api/exports/{id} — single export job by ID
[x] (P1) Export page — validation summary, excluded tracks table, run panel, history
[x] (P1) ErrorBanner onDismiss prop + TS build fixed


----------------------------------------------------------------------
SSD SYNC / JOB CANCELLATION / PROGRESS (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) process_registry — shared subprocess table; SIGTERM cancel for all job types
[x] (P1) Job model extended: pid, progress_current/total/percent/message, 'cancelled' status
[x] (P1) DB migration: _add_column_safe() — idempotent ALTER TABLE on startup
[x] (P1) job_service: mark_pid(), mark_progress(), clear_pid()
[x] (P1) toolkit_runner: registers process, stores PID, checks cancellation flag on exit
[x] (P1) rsync_runner: preview_sync (dry-run), start_sync_job, _run_rsync_job with
         progress parsing from --info=progress2 (to-chk=N/M, throttled 1s writes)
[x] (P1) POST /api/jobs/{id}/cancel — SIGTERM + 'cancelled' status
[x] (P1) GET /api/sync/config, POST /api/sync/preview, POST /api/sync/run
[x] (P1) GET /api/sync, GET /api/sync/{id}
[x] (P1) SsdSync page — config panel, preview list, run panel (allow_delete opt-in),
         active job with progress bar + cancel, sync history
[x] (P1) Jobs page — Cancel button on running rows, inline progress bar
[x] (P1) CSS: cancelled badge, danger button, progress bars, sync page classes

[ ] (P2) POST /api/jobs/{id}/cancel: handle SIGKILL fallback if SIGTERM is ignored after timeout
[ ] (P2) GET /api/jobs/{id}/logs: SSE streaming for live log tail (currently polls 2s)
[ ] (P2) WebSocket /ws/jobs/{id}/logs — real-time log streaming
[ ] (P2) rsync --bwlimit option for bandwidth throttling during sync
[ ] (P3) Job retention policy — auto-clean logs older than N days
[ ] (P3) SSD Sync: show last-synced timestamp from most recent succeeded sync job


----------------------------------------------------------------------
BACKEND — PHASE 1 (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) FastAPI backend skeleton
    Files: backend/app/main.py, backend/app/core/config.py, backend/app/core/db.py

[x] (P1) Routes: GET /api/health, GET /api/version
    File: backend/app/api/routes/health.py

[x] (P1) Routes: GET /api/jobs, GET /api/jobs/{id}, GET /api/jobs/{id}/logs, POST /api/jobs
    File: backend/app/api/routes/jobs.py

[x] (P1) SQLite-backed Job model (backend/data/jobs.db — separate from pipeline DB)
    Files: backend/app/models/job.py, backend/app/schemas/job.py,
           backend/app/core/db.py, backend/app/services/job_service.py

[x] (P1) toolkit_runner service — safe subprocess execution with allowlists
    File: backend/app/services/toolkit_runner.py
    - ALLOWED_COMMANDS (18 subcommands)
    - _ALLOWED_BOOL_FLAGS, _ALLOWED_VALUE_FLAGS with value validators
    - Commands built as argument lists; never shell strings
    - asyncio background tasks with task registry (prevents GC)

[x] (P1) stdout+stderr → log files; /logs endpoint returns file contents + ?tail=N
[x] (P2) README Backend section with run commands and API table


----------------------------------------------------------------------
BACKEND — PHASE 2 (open)
----------------------------------------------------------------------
[x] (P1) Frontend: React/Vite/TS shell — entry point, routing, CSS, Settings page
    Files: frontend/src/main.tsx, frontend/src/App.tsx,
           frontend/src/pages/Settings.tsx, frontend/src/index.css
    Note: node/npm must be installed on the host to run
[ ] (P2) POST /api/jobs: support --path override (validated absolute path)
[ ] (P2) GET /api/jobs/{id}/logs: SSE streaming for live log tail
[ ] (P2) POST /api/jobs/{id}/cancel — SIGTERM the subprocess if still running
[x] (P2) GET /api/tracks, /stats, /issues, /{id} — read-only track browsing from pipeline DB
    Files: backend/app/core/pipeline_db.py, backend/app/models/track.py,
           backend/app/schemas/track.py, backend/app/services/track_service.py,
           backend/app/api/routes/tracks.py
[x] (P2) Frontend Tracks page — search/filter/sort/paginate + slide-in detail panel
    Files: frontend/src/types/track.ts, frontend/src/api/tracks.ts,
           frontend/src/hooks/useTracks.ts, frontend/src/pages/Tracks.tsx,
           frontend/src/components/TrackPanel.tsx
[x] (P1) BPM anomaly detection + review workflow
    Backend: bpm_analysis.py service, analysis.py routes, bpm_anomalies table in jobs.db
    Frontend: BpmReview page, types/analysis.ts, api/analysis.ts
[x] (P2) Set Builder UI — form + job dispatch + playlist history + track detail
    Backend: schemas/playlist.py, services/playlist_service.py,
             api/routes/playlists.py — POST /set-builder, GET /playlists, GET /playlists/{id}
    Frontend: types/playlist.ts, api/playlists.ts, pages/SetBuilder.tsx
    Also: toolkit_runner.py extended with all set-builder flags
[ ] (P2) BPM anomaly: per-track reanalyze (requires pipeline support for --track-id)
[ ] (P2) POST /api/jobs: support --path override (validated absolute path)
[ ] (P2) GET /api/jobs/{id}/logs: SSE streaming for live log tail
[ ] (P2) POST /api/jobs/{id}/cancel — SIGTERM the subprocess if still running
[ ] (P3) WebSocket /ws/jobs/{id}/logs — real-time log streaming
[ ] (P3) Job retention policy — auto-clean logs older than N days


----------------------------------------------------------------------
ARTIST MERGE (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) Fix _classify_merge() normalization bug
    Old code compared fi.primary_artist.lower() directly; "Heavy-K" and "Heavy K"
    produced different lowercase strings → incorrectly flagged as AMBIGUOUS.
    Fix: _normalize_primary_for_compare() applies same transforms as normalize_artist_key()
    (strip periods, hyphen→space) before comparison.
    File: modules/artist_merge.py

[x] (P1) Three-category merge classification
    SAFE_ALIAS          — formatting/punctuation/casing differences only
    SAME_PRIMARY_COLLAB — same primary after normalization; ≥1 folder has collab suffix
    AMBIGUOUS           — primary artists genuinely differ; never auto-merged
    File: modules/artist_merge.py

[x] (P2) _describe_alias_differences() helper
    Generates human-readable reason strings for SAFE_ALIAS groups:
    "capitalization variant", "dotted-initials variant", "hyphen/space variant",
    "trailing period variant", "period variant", etc.
    File: modules/artist_merge.py

[x] (P2) _pick_canonical() all-uppercase penalty
    All-uppercase canonical names (HOSH) penalized; mixed-case preferred (H.O.S.H).
    File: modules/artist_merge.py

[x] (P1) Tests for artist merge (77 tests)
    File: tests/test_artist_merge.py
    Covers: extract_primary_artist, _has_collab_suffix, normalize_artist_key,
    _normalize_primary_for_compare, _describe_alias_differences, _classify_merge,
    _pick_canonical — all variant types: punctuation, casing, hyphen/space,
    trailing period, dotted initials, feat/ft/featuring, comma-collab, ambiguous


----------------------------------------------------------------------
DEV EXPERIENCE (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P2) direnv support — .envrc activates .venv on cd into project
[x] (P2) requirements-dev.txt — separate dev deps; -r requirements.txt + pytest
[x] (P2) requirements.txt — added tqdm, argcomplete, anthropic
[x] (P2) venv warning in pipeline.py — advisory stderr message if no venv active
[x] (P2) argcomplete wiring — PYTHON_ARGCOMPLETE_OK + autocomplete(parser)
[x] (P2) README Developer Setup section — direnv + autocomplete activation docs


----------------------------------------------------------------------
DOCS / CLI HELP (completed 2026-04-12)
----------------------------------------------------------------------
[x] (P1) Centralized command registry (modules/doc_registry.py)
    Single source of truth for all commands, flags, examples.
    Used to generate COMMANDS.txt, README.md sections, COMMANDS.html.

[x] (P1) generate-docs subcommand
    python3 pipeline.py generate-docs [--dry-run] [--format ...] [--output-dir DIR]
    Regenerates COMMANDS.txt, README.md (spliced), COMMANDS.html.

[x] (P1) validate-docs subcommand
    python3 pipeline.py validate-docs [--strict]
    Reports missing/stale commands. --strict exits 1 for CI/pre-commit use.

[x] (P2) Pre-commit hook script
    scripts/pre_commit_validate_docs.sh — copy to .git/hooks/pre-commit.
    Blocks commits when docs are out of sync with the registry.

[ ] (P3) When adding a new subcommand:
    1. Add entry to modules/doc_registry.py REGISTRY list
    2. Run: python3 pipeline.py generate-docs
    3. Commit COMMANDS.txt, README.md, COMMANDS.html alongside pipeline.py changes


----------------------------------------------------------------------
ANALYSIS / CORE PIPELINE
----------------------------------------------------------------------
[ ] (P1) Validate _read_existing_analysis() against real MIK-tagged files
    Test with MP3, FLAC, M4A files that MIK has already processed.
    Confirm TKEY / TBPM / INITIALKEY / tmpo are read correctly.
    File: modules/analyzer.py

[ ] (P1) Tagger: skip BPM/key writes when values came from MIK (no-op write prevention)
    Currently tagger.py writes BPM/key from DB on every run.
    If MIK values were promoted to DB by analyzer, tagger may re-write them
    (harmless but noisy). Add a "source=mik" marker to DB to detect this.

[ ] (P1) Energy tag write-back
    Write energy tier (Peak/Mid/Chill) into tags:
      - MP3: TXXX:ENERGY
      - FLAC: ENERGY
    Enables Rekordbox filtering and smart playlists.

[ ] (P2) BPM normalization layer (lower priority now that MIK is authoritative)
    For tracks MIK has not processed, BPM scaling issues still possible.
    Implement normalization if/when needed as fallback only.
    File: modules/analyzer.py


----------------------------------------------------------------------
CONVERT AUDIO
----------------------------------------------------------------------
[ ] (P2) Support 24-bit AIFF output for ALAC source M4A
    Currently always uses pcm_s16be regardless of source bit depth.
    Detect ALAC codec via ffprobe streams and use pcm_s24be for lossless M4A.
    File: modules/convert_audio.py

[ ] (P2) Post-convert MIK analysis trigger
    After convert-audio completes, optionally queue the new .aiff files for
    Mixed In Key (via a shell open or a file watch trigger).
    Blocked on MIK offering a CLI interface.

[ ] (P3) convert-audio: configurable output codec via --codec flag
    Allow pcm_s16be / pcm_s24be / pcm_f32be for advanced use cases.
    File: modules/convert_audio.py


----------------------------------------------------------------------
REKORDBOX / EXPORT
----------------------------------------------------------------------
[ ] (P2) Rekordbox hot-cue write-back (blocked by MIK-first policy)
    MIK is now the authoritative cue source.
    If/when implementing: must only write cues to tracks MIK has NOT touched.
    Must never overwrite POSITION_MARK entries already in the XML.
    Map:
      intro_start → cue 1
      drop → cue 2
      outro_start → cue 3

[ ] (P2) Route tracks in letter playlists
    Decide:
      - Exclude route folders OR
      - Assign correct letter instead of '#'
    Validate behaviour in Rekordbox UI first.

[ ] (P2) Windows transfer automation
    Automate sync of /music → SSD:
      - rsync or dedicated subcommand
      - Trigger rekordbox-export after sync


----------------------------------------------------------------------
LABEL INTELLIGENCE
----------------------------------------------------------------------
[ ] (P2) label-clean Phase 2: Discogs provider
    Implement Discogs API lookup
    File: label_intel/providers/discogs.py

[ ] (P2) label-clean Phase 2: Beatport provider
    Implement single-track label resolution
    (separate from scraper)


----------------------------------------------------------------------
PIPELINE INFRASTRUCTURE
----------------------------------------------------------------------
[x] (P1) Automatic persistent run logging for intelligence pipeline (2026-04-20)
    .log + .jsonl + _summary.json written to logs/<command>/ on every run.
    RunLogger in utils/prompt_logger.py; --log-dir flag on all 4 commands.
    Files: config.py, utils/prompt_logger.py, pipeline.py
[x] (P1) Production-grade observability: paged detail files + rich summary JSON (2026-04-21)
    record_outcome() → modified/skipped/errors paged JSON (max 200/page).
    inc()/set_counter() → nested results/ai/quality/sanitize summary.
    Pre-sanitize context detection (sanitize_changed/sanitize_clean prefixes).
    Memory-safe for 8000+ file runs. Files: all 6 modified.

[ ] (P2) Per-run config snapshots
    Store config JSON in pipeline_runs table
    Enables debugging of historical runs

[x] (P2) Local AI metadata normalization (ai-normalize subcommand, 2026-04-15)
    Ollama-backed; preview-first; safe write with --apply + --min-confidence gate.
    Files: ai/__init__.py, ai/ollama_client.py, ai/metadata_schema.py, ai/normalizer.py
    Config keys: OLLAMA_BASE_URL, OLLAMA_DEFAULT_MODEL, OLLAMA_TIMEOUT
[x] (P1) ai-normalize production hardening (2026-04-19)
    MIN_AI_CONFIDENCE=0.80 constant; NormalizeResult type; structured rejection reasons
    (low_confidence, schema_invalid, guardrail_violation, parsed_conflict, ai_error);
    _check_parsed_alignment(); _apply_hard_guards returns tuple; guardrail_fired flag;
    all rejections logged at INFO; JSON output includes rejection_reason.

[ ] (P3) ai-normalize: add --resume / skip-already-normalized flag
    Skip files whose tags were already written by a previous --apply run
    (detect via a TXXX:AI_NORMALIZED custom ID3 marker, similar to TXXX:PROCESSED)

[ ] (P3) ai-normalize: batch mode with rate limiting for large libraries
    Currently processes one file at a time. Could parallelize with a thread pool
    and add a --workers flag (safe since Ollama handles concurrent requests).

[ ] (P3) LLM metadata assist integration via Claude API
    Use Claude for genre inference / metadata disambiguation (cloud fallback
    when Ollama is not available). Wire through utils/llm_client.py.
    File: utils/llm_client.py


----------------------------------------------------------------------
DISCOVERED DURING SESSION (2026-04-07)
----------------------------------------------------------------------
- MIK-first policy now enforced; analyzer _read_existing_analysis() needs
  real-file validation before relying on it in production
- convert-audio codec is hard-coded to pcm_s16be; ALAC M4A sources would
  benefit from pcm_s24be to avoid lossy downsampling


----------------------------------------------------------------------
AUDIT-QUALITY FOLLOW-UPS
----------------------------------------------------------------------
[ ] (P2) set-builder: honour quality_tier — exclude LOW tracks from auto sets
    Filter LOW from the set-builder candidate pool, or add --exclude-low flag.
    File: modules/set_builder.py + pipeline.py set-builder subcommand

[ ] (P3) audit-quality: quality-tier playlists
    Generate LOW.m3u8 / MEDIUM.m3u8 / HIGH.m3u8 / LOSSLESS.m3u8 under M3U_DIR/Quality/
    Useful for reviewing problem tracks in Rekordbox.
    File: modules/playlists.py or audit_quality.py

[ ] (P3) audit-quality: re-check already-tagged files incrementally
    If quality_tier is already set in DB and file is unchanged, skip re-probe.
    Speeds up repeated runs on large libraries.
    File: modules/audit_quality.py


----------------------------------------------------------------------
DONE
----------------------------------------------------------------------
[x] Initial pipeline implementation
[x] CLI subcommands
[x] SQLite schema
[x] Label intelligence scraper
[x] Rekordbox export
[x] MIK-first refactor (v1.1.0)
[x] convert-audio subcommand: .m4a → .aiff (v1.2.0)
[x] Sanitizer: strip leading track-index junk before pipe (v1.3.0)
[x] audit-quality subcommand: codec/bitrate audit, report, move, tag (v1.4.0)
[x] Artist merge: three-category classification + normalization fix + 77 tests (v1.7.0)


======================================================================
END OF NEXT TASKS
======================================================================
