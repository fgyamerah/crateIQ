======================================================================
CRATEIQ — NEXT TASKS / OPEN WORK
======================================================================

Legend:
[ ] = open
[~] = in progress

Priority:
P0 = critical (affects core functionality)
P1 = high
P2 = medium
P3 = low

Completed work is not tracked here. See CHANGELOG.txt for the dated
change history and docs/history/ for summarized completed-cycle
narrative. This file is the active work queue only. The Task 27–30 P1
closeout is complete; no newly evidenced P0/P1 blocker was found.

----------------------------------------------------------------------
P0 / CRITICAL
----------------------------------------------------------------------

(none currently open)

----------------------------------------------------------------------
P1 / HIGH
----------------------------------------------------------------------

(none currently open)

----------------------------------------------------------------------
P2 / MEDIUM
----------------------------------------------------------------------

[ ] Two narrow backend reads of the legacy root `config.py` remain after
    the pipeline/config dependency-isolation cycle and are intentionally
    unchanged for now: the backend version string and the export-context
    display values. Revisit only if an explicit runtime config source is
    introduced for those surfaces.

[ ] Legacy Direct Library removal preconditions remain open. Do not plan a
    removal date until the current FastAPI equivalents or explicit drops
    exist for the remaining toolkit-runner-compatible commands, the
    compatibility wizard is proven unused or migrated, and the remaining
    pipeline/config/db dependencies are audited.

[ ] Add a derived `outcome` (matching the analysis_operations `derive_outcome`
    pattern) to preparation_operations and waveform_operations read paths so
    `status='completed'` with a non-zero `failed_count` cannot render as
    plain success. Do not change persisted status columns. A read-only
    adapter in the service layer is sufficient.

[ ] Implement a read-only shared operation-list adapter function in the
    backend service layer that normalizes the diverging count-field names
    (succeeded/generated/written_count/applied_count) and status vocabularies
    (`completed` vs `succeeded`) into the minimum common vocabulary defined
    in `docs/architecture/OPERATION_RESULT_CONTRACT.md` Section 3.1. This is
    a read-side only change with no schema migration.

[ ] Needs Review still lacks a cross-category bulk acceptance flow for
    high-confidence items. Design the review semantics before adding any
    bulk mutation path so approval and execution stay separate.

[ ] Credential-backed provider coverage still needs a deliberate review for
    the currently active credential-gated providers once real credentials
    are configured. Keep legacy provider names out of the active set.

[ ] Resolve the FastAPI/Starlette `TestClient` HTTP-client deprecation and
    plan the current frontend build-tool upgrade path without changing the
    product contract.

[ ] Before implementing duplicate-resolution execution, exercise CrateIQ's
    managed Inbox/Library workflow with real-world local usage and review
    whether the planned held-operation read model is understandable. The
    design is complete in `docs/architecture/DUPLICATE_RESOLUTION_SPEC.md`,
    but the runtime remains read-only/plan-only; do not implement execution
    without a separately approved task and disposable-root acceptance tests.

----------------------------------------------------------------------
P3 / LOW
----------------------------------------------------------------------

[ ] Set Builder refinement: add a real draft/reorder/notes/readiness
    workspace around the current functional flow. Keep the current safe
    job-backed behavior intact and do not invent unsupported compatibility
    scoring.

[ ] Library workspace follow-ups: URL-backed reproducible views plus a
    stronger provenance/history inspector if the current local-storage
    state proves too limited for replayable workflows.

[ ] Accessibility / responsive / performance follow-up work: browser-backed
    verification, keyboard and touch polish, and measured large-library
    performance checks.

[ ] Release-readiness hardening after the test foundation exists: route
    smoke coverage, deterministic fixture-based end-to-end checks, and a
    release checklist that reflects the actual supported routes and
    contracts.

----------------------------------------------------------------------
DEFERRED / PARKED
----------------------------------------------------------------------

[ ] Queue JSON/JSONL mutation. No concrete queue schema contract is approved,
    so queue findings remain non-executable; keep this separate from the
    completed DB-only reference-artifact writers.

[ ] Structured artist/title/version metadata representation. No concrete
    current consumer exists, so adding dead schema now would be premature.
    Revisit only if a real version-aware workflow needs it.

[ ] Explicit `existing_local_value` provenance backfill for tracks that
    predate `field_provenance`. No current workflow depends on baseline
    provenance existing for already-indexed tracks, so keep it parked until
    a concrete consumer appears.
